independent_first_frame: false
warp_denoising_step: false
weight_decay: 0.01
same_step_across_blocks: true
# pin the DMD backward-simulation exit step to the last denoising step instead of
# sampling it (pipeline/self_forcing_control_causal_training.py:65)
last_step_only: false
i2v: false
num_training_frames: 21
gc_interval: 100
context_noise: 0
